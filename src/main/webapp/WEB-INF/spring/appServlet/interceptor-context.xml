<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        https://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/mvc
        https://www.springframework.org/schema/mvc/spring-mvc.xsd">

    <!-- Interceptor 등록 -->
    <mvc:interceptors>

        <!-- 전체 공통 Interceptor 예시 -->
        <mvc:interceptor>
            <!-- 적용 URL -->
            <mvc:mapping path="/**"/>

            <!-- 제외 URL (정적 리소스, 에러 페이지 등) -->
            <mvc:exclude-mapping path="/resources/**"/>
            <mvc:exclude-mapping path="/css/**"/>
            <mvc:exclude-mapping path="/js/**"/>
            <mvc:exclude-mapping path="/assets/**"/>
            <mvc:exclude-mapping path="/error/**"/>

            <!-- 실제 Interceptor 클래스 -->
            <bean class="net.planet.calendar.common.interceptor.CommonInterceptor"/>
        </mvc:interceptor>

        <!-- 로그인 체크용 Interceptor 예시 (나중에 사용) -->
        <!--
        <mvc:interceptor>
            <mvc:mapping path="/secure/**"/>
            <mvc:exclude-mapping path="/login"/>
            <mvc:exclude-mapping path="/logout"/>
            <bean class="net.planet.calendar.common.interceptor.LoginInterceptor"/>
        </mvc:interceptor>
        -->

    </mvc:interceptors>

</beans>
